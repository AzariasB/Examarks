{# empty Twig template #}
{% extends "base.html.twig" %}

{% block title %}
    Lobby - Teacher
{% endblock %}

{% block content %}
    <div class="container">
        <h1>Welcome back, {{ app.user.username }}</h1>
        <div class="row" >
            <div class="col-xs-12" id='modules'>

                        <a href="{{ path('studentList') }}" class="btn btn-primary"><span class="glyphicon glyphicon-user"></span>
                            {% if is_granted('ROLE_ADMIN') %}
                                 Users
                            {% else %}
                                 Students
                            {% endif %}
                        </a>                                
                        <a href="{{ path('moduleList') }}" class="btn btn-primary"><span class="glyphicon glyphicon-book"></span> Modules</a>
                        <a href="{{ path('surveyResult') }}" class="btn btn-primary"><span class="glyphicon glyphicon-signal"></span> Surveys</a>
            </div>
        </div>
    </div>
{% endblock %}